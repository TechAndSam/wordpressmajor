<?php
$number_of_chat=$get_number_of_chat;
for ($left_sidebar=0; $left_sidebar<$number_of_chat; $left_sidebar++)
{
	if ($contact[0]=='+')
	{
		if ($right_content contains $image and $caption)
		{
			$caption;
			$number_of_occurence++;
		}
	}
}
echo $caption.', '.$number_of_occurence; //Save data in txt file or database
for ($left_sidebar=0; $left_sidebar<$number_of_chat; $left_sidebar++)
{
	if ($contact[0]=='+')
	{
		rename('+23400000000000', '$left_sidebar');
	}
}


?>
Scroll Thru the first Chat
	If the contact starts with +.
		If yes, check if it has an image and a caption.
		If yes, copy the caption(Phone Number) and save the caption. continue
		If no, continue 

Scroll Thru the second Chat
	If the contact starts with +.
		If yes, check if it has an image and a caption.
		If yes, copy the caption(Phone Number) and save the caption. continue
		If no, continue 

Scroll Thru the first Chat
	If the contact starts with +.
		If yes, check if it has an image and a caption.
		If yes, copy the caption(Phone Number) and save the caption. continue
		If no, continue 


	Check if it has an image and a caption. 
	. Then loop to the next chat.
	If no, ignore and move to the next chat or number
